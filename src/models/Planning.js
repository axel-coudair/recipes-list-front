export default class House {
	constructor(id, date, recipeId, houseId, ingredients) {
		this.id = id
		this.date = date
		this.houseId = houseId
		this.recipeId = recipeId
		this.ingredients = ingredients
	}
}
